#笔记
1.希望直接链接外网的服务，但不好配置还是放弃，在本地创建开发环境mysql,每日的数据库修改再同步到外网的正式环境。
1）开发环境和正式环境的数据库内数据是不相同的，所以开发过程中有字段修改，直接在正式环境修改就行了。

2.itheima是啥意思这是黑马的意思。

3.我想在的项目中有tomcat吗，有没有web运行环境，在哪个jar包里

4.ORM对象关系映射，将数据库表和实体类的属性对应起来，通过实体类可以操作数据库。

5.创建mybatis的主配置文件SqlMapConfig.xml。创建映射配置文件

6.resources目录下，基本是配置文件，菲java软件。映射配置文件是每个dao独立的配置文件

7.加载的过程是什么,我这里应该是web应用的，只能通过main方法，对数据进行增删改查

8.目录创建和包创建是不一样的,我们写这样好处，开发不需要写dao的实现类，